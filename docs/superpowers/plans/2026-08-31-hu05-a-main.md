# HU-05 hacia main Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrar exclusivamente HU-05 sobre el `main` que ya contiene HU-04, sin reintroducir historia duplicada ni perder comportamiento existente.

**Architecture:** La rama `integration/hu-05-a-main` parte de `origin/main`. Se aplica solo `ce05df7` mediante cherry-pick y se resuelve semánticamente el único conflicto previsto en la raíz de composición de NestJS, conservando en paralelo los proveedores de recuperación HU-04 y los de Mi Cuenta HU-05.

**Tech Stack:** Node.js 24, TypeScript 5.9, NestJS 11, Jest 30, Kysely, PostgreSQL 17/Testcontainers, Docker y GitHub Actions.

---

### Task 1: Proteger la base y aplicar exclusivamente HU-05

**Files:**
- Verify: `docs/superpowers/specs/2026-08-31-hu05-a-main-design.md`
- Apply from commit: `ce05df73becff2b36423c1bc32ccb44f1d53d916`
- Conflict expected: `src/infrastructure/bootstrap/app.module.ts`

- [ ] **Step 1: Confirmar que el workspace está limpio y la rama contiene el main vigente**

Run:

```powershell
git fetch --no-tags origin main develop
git status --short --branch
git merge-base --is-ancestor origin/main HEAD
git rev-parse origin/main origin/develop HEAD
```

Expected: estado limpio, `git merge-base` con exit 0 y `HEAD` basado en `origin/main`. Si `origin/main` avanzó, integrar primero ese avance y repetir la simulación; no continuar con hashes obsoletos.

- [ ] **Step 2: Reconfirmar que los commits duplicados no deben transferirse**

Run:

```powershell
git cherry -v origin/main origin/develop
git show -s --format='%H %T' a5c45b3 b595b15 66d5b64 8a21f29 cf2c60d 01c3e91
```

Expected: `66d5b64` marcado con `-`; los pares `a5c45b3`/`8a21f29`, `b595b15`/`cf2c60d` y `66d5b64`/`01c3e91` conservan árboles iguales. El único commit funcional a aplicar es `ce05df7`.

- [ ] **Step 3: Aplicar HU-05 sin traer la historia completa de develop**

Run:

```powershell
git cherry-pick ce05df73becff2b36423c1bc32ccb44f1d53d916
git status --short
git diff --name-only --diff-filter=U
```

Expected: cherry-pick detenido únicamente por conflicto en `src/infrastructure/bootstrap/app.module.ts`; ningún otro archivo aparece con estado `U`.

### Task 2: Combinar HU-04 y HU-05 en la raíz de composición

**Files:**
- Modify: `src/infrastructure/bootstrap/app.module.ts`
- Test: `test/integration/account-self-service-http.spec.ts`
- Regression: `test/integration/recovery-http.spec.ts`

- [ ] **Step 1: Resolver imports, tokens y controladores conservando ambas historias**

En `app.module.ts`, eliminar los marcadores de conflicto y conservar los imports HU-04 existentes. Añadir o conservar estas referencias HU-05 exactas:

```typescript
import { PasswordController } from '../../adapters/inbound/http/password.controller'
import { ChangeOwnPassword } from '../../application/use-cases/ChangeOwnPassword'
import { UpdateOwnAccount } from '../../application/use-cases/UpdateOwnAccount'
import {
  PASSWORD_CHANGE,
  type PasswordChangePort,
} from '../../application/ports/PasswordChangePort'
import { CognitoPasswordChange } from '../../adapters/outbound/identity/CognitoPasswordChange'
import { InMemoryPasswordChange } from '../../adapters/outbound/identity/InMemoryPasswordChange'
```

El import desde `tokens.ts` debe contener simultáneamente:

```typescript
GET_OWN_ACCOUNT,
UPDATE_OWN_ACCOUNT,
CHANGE_OWN_PASSWORD,
RESET_RECOVERY_PASSWORD,
START_PASSWORD_RECOVERY,
VERIFY_RECOVERY_ANSWERS,
VERIFY_RECOVERY_CODE,
```

La lista de controladores debe contener ambos módulos funcionales:

```typescript
controllers: [
  AccountsController,
  RecoveryController,
  MfaController,
  PasswordController,
  SessionsController,
  HealthController,
],
```

- [ ] **Step 2: Conservar los proveedores de recuperación y añadir el cambio de contraseña**

Mantener sin cambios los proveedores HU-04 `RECOVERY_CHALLENGE_REPOSITORY`, `RECOVERY_OTP`, `IDENTITY_PASSWORD_RESET`, `START_PASSWORD_RECOVERY`, `VERIFY_RECOVERY_ANSWERS`, `VERIFY_RECOVERY_CODE` y `RESET_RECOVERY_PASSWORD`.

Añadir el proveedor de infraestructura HU-05:

```typescript
{
  provide: PASSWORD_CHANGE,
  useFactory: (config: AppConfig, logger: Logger): PasswordChangePort => {
    if (config.cognito === null) {
      logger.warn('password_change', {
        driver: 'memoria',
        detail: 'Sin proveedor de identidad: el cambio de contrasena no llega a Cognito.',
      })

      return new InMemoryPasswordChange()
    }

    logger.info('password_change', { driver: 'cognito' })

    return new CognitoPasswordChange({ userPoolId: config.cognito.userPoolId })
  },
  inject: [APP_CONFIG, LOGGER],
},
```

Añadir los casos de uso HU-05:

```typescript
{
  provide: UPDATE_OWN_ACCOUNT,
  useFactory: (
    accounts: AccountRepositoryPort,
    blacklist: NicknameBlacklistPort,
  ): UpdateOwnAccount => new UpdateOwnAccount(accounts, blacklist),
  inject: [ACCOUNT_REPOSITORY, NICKNAME_BLACKLIST],
},
{
  provide: CHANGE_OWN_PASSWORD,
  useFactory: (passwords: PasswordChangePort): ChangeOwnPassword =>
    new ChangeOwnPassword({ passwords }),
  inject: [PASSWORD_CHANGE],
},
```

- [ ] **Step 3: Verificar estructuralmente la resolución antes de continuar**

Run:

```powershell
rg -n '<<<<<<<|=======|>>>>>>>' src/infrastructure/bootstrap/app.module.ts
rg -n 'RecoveryController|PasswordController|RECOVERY_CHALLENGE_REPOSITORY|IDENTITY_PASSWORD_RESET|PASSWORD_CHANGE|UPDATE_OWN_ACCOUNT|CHANGE_OWN_PASSWORD' src/infrastructure/bootstrap/app.module.ts
npm run typecheck
```

Expected: la primera búsqueda no encuentra marcadores; la segunda encuentra todos los símbolos HU-04 y HU-05; TypeScript termina con exit 0.

- [ ] **Step 4: Completar el cherry-pick como un único commit funcional trazable**

Run:

```powershell
git add -- src/infrastructure/bootstrap/app.module.ts
git cherry-pick --continue
git status --short --branch
git log -3 --oneline --decorate
```

Expected: workspace limpio y un nuevo commit HU-05 encima de los commits documentales de diseño y plan, sin commits `a5c45b3`, `b595b15` ni `66d5b64` en la rama.

### Task 3: Demostrar que HU-05 funciona y HU-04 permanece

**Files:**
- Test: `test/unit/change-own-password.spec.ts`
- Test: `test/unit/update-own-account.spec.ts`
- Test: `test/integration/account-self-service-http.spec.ts`
- Regression: `test/unit/password-recovery.spec.ts`
- Regression: `test/integration/recovery-http.spec.ts`
- Regression: `test/db/postgres-recovery-challenge-repository.spec.ts`

- [ ] **Step 1: Ejecutar las pruebas enfocadas de ambas historias**

Run:

```powershell
npm run test:unit -- --runInBand test/unit/change-own-password.spec.ts test/unit/update-own-account.spec.ts test/unit/password-recovery.spec.ts
npm run test:integration -- --runInBand test/integration/account-self-service-http.spec.ts test/integration/recovery-http.spec.ts
```

Expected: todas las suites y pruebas seleccionadas pasan; HU-05 no rompe el flujo de recuperación HU-04.

- [ ] **Step 2: Validar que el diff no revierte archivos de main**

Run:

```powershell
git merge-base --is-ancestor origin/main HEAD
git diff --check origin/main...HEAD
git diff --name-status origin/main...HEAD
git log --oneline origin/main..HEAD
```

Expected: `origin/main` es ancestro; no hay whitespace ni marcadores; el historial adicional contiene solo el documento y HU-05. Los archivos HU-04 añadidos en `9a7a33a` siguen presentes.

### Task 4: Ejecutar la verificación completa local

**Files:**
- Verify: `package.json`
- Verify: `.github/workflows/ci.yml`
- Verify: `Dockerfile`

- [ ] **Step 1: Activar Node.js 24 e instalar exactamente el lockfile**

Run:

```powershell
fnm use 24.14.0
node --version
npm ci
```

Expected: Node `v24.14.0` y `npm ci` con exit 0.

- [ ] **Step 2: Ejecutar la secuencia completa del job de calidad**

Run:

```powershell
$ciScripts = @('lint','format:check','typecheck','test:unit','test:integration','test:db','test:coverage','build')
foreach ($ciScript in $ciScripts) {
  npm run $ciScript
  if ($LASTEXITCODE -ne 0) { throw "Fallo npm run $ciScript con codigo $LASTEXITCODE" }
}
```

Expected: todos los scripts terminan con exit 0, incluidas las pruebas PostgreSQL reales y los umbrales de cobertura.

- [ ] **Step 3: Construir y probar la imagen local**

Run:

```powershell
docker build --tag nexus-battle-account:hu05-main .
docker run --rm --entrypoint sh nexus-battle-account:hu05-main -c 'test -n "$AVATAR_STORAGE_PATH" && touch "$AVATAR_STORAGE_PATH/.prueba" && rm -f "$AVATAR_STORAGE_PATH/.prueba"'
```

Expected: imagen construida y ruta de avatares escribible por el usuario de ejecución.

### Task 5: Publicar la rama y abrir el PR

**Files:**
- Verify: `docs/superpowers/specs/2026-08-31-hu05-a-main-design.md`
- Verify: `docs/superpowers/plans/2026-08-31-hu05-a-main.md`

- [ ] **Step 1: Confirmar el estado final antes de publicar**

Run:

```powershell
git status --short --branch
git diff --check origin/main...HEAD
git log --oneline origin/main..HEAD
```

Expected: workspace limpio; solo commits documental, plan y HU-05 sobre `origin/main`.

- [ ] **Step 2: Publicar sin force-push**

Run:

```powershell
git push --set-upstream origin integration/hu-05-a-main
```

Expected: rama remota creada mediante avance normal.

- [ ] **Step 3: Crear el PR hacia main**

Run:

```powershell
$prBody = @'
## Resumen

- integra exclusivamente HU-05 sobre el main que ya contiene HU-04
- conserva recuperación de contraseña y añade Mi Cuenta/cambio de contraseña
- evita los commits duplicados de sincronización y HU-03 de develop

## Validación

- lint, formato, tipos, unitarias, integración, PostgreSQL real y cobertura
- build del servicio e imagen Docker
- comprobación de que origin/main permanece como ancestro
'@
gh pr create --repo Nexus-Battle-VI/Nexus-Battle-Account --base main --head integration/hu-05-a-main --title "feat(account): [HU-05] integrar Mi Cuenta sobre main" --body $prBody
```

Expected: URL del nuevo PR dirigido a `main`.

- [ ] **Step 4: Esperar los checks y confirmar política de revisión**

Run:

```powershell
gh pr checks --repo Nexus-Battle-VI/Nexus-Battle-Account --watch --interval 10
gh pr view --repo Nexus-Battle-VI/Nexus-Battle-Account --json url,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup
```

Expected: checks de calidad, CodeQL e imagen en verde; publicación GHCR omitida en PR; estado fusionable y, como máximo, pendiente de revisión humana.
